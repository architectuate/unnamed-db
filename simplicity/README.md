There is beauty in simplicity
=============================

There are times when a single shell script will do. This is one of those times.

The simplest database - a CLI-based key-value store, appends to and reads from a single text file.
Not very efficient, file could grow exponentially, every look-up scans the entire file.